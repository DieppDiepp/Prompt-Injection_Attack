# M5 TypeScript SDK Summary

Status: passed for MVP

The first AIRC client is a thin HTTP wrapper with no Encore dependency. Hosted
agents can also implement the documented JSON webhook contract directly.
