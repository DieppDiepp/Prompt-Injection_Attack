# Local use only. Never commit a real .env file.
OPENAI_API_KEY=your_openai_api_key
DEEPSEEK_API_KEY=your_deepseek_api_key

# Optional: a server-only reference for the GPT-4o-mini judge. It is never shown
# in the UI or stored in the database. Use a FILE for a long system prompt, and never commit that file.
# INJECTION_JUDGE_REFERENCE_FILE=C:\secure\partner-system-prompt.txt
