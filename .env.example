# Chỉ dùng cho môi trường local. Không commit file .env thật.
OPENAI_API_KEY=your_openai_api_key

# Tuỳ chọn: tham chiếu chỉ ở server cho GPT-4o-mini judge, không xuất hiện ở UI
# và không lưu trong database. Dùng FILE cho system prompt dài, không commit file đó.
# INJECTION_JUDGE_REFERENCE_FILE=C:\secure\partner-system-prompt.txt
